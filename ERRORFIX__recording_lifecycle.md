## [2026-07-21 17:58:52] [Session ID: omx-1784512435044-92wxat] 错误修复: restart 决策重复落盘

### 现象

- `task_plan__recording_lifecycle.md` 与 `notes__recording_lifecycle.md` 各出现两条内容相同的 restart 决策。
- 时间戳分别为 `15:28:16` 与 `17:35:13`,且追加顺序与时间顺序不一致。

### 原因

- 用户在状态读取与决定落盘之间发送“继续”,恢复流程误判前一次写入尚未发生,导致相同 patch 被再次追加。

### 修复

- 不修改或删除 append-only 历史记录。
- 在计划和笔记末尾追加校正锚点,明确两条记录只代表一次用户决定。
- 后续恢复时必须先读取文件末尾,以最新校正锚点作为当前状态。

### 验证

- 已读取两个文件末尾,确认重复内容完全一致,不存在协议冲突。
- 最新计划状态已推进到 completed Bundle retention policy,不再停留于 restart 决策。

## [2026-07-21 18:20:10] [Session ID: omx-1784512435044-92wxat] 错误修复: 校正 patch 未追加到真实 EOF

### 现象

- `17:58:52` 的校正内容出现在第二条重复 restart 记录之前,未成为文件末尾最终锚点。

### 原因

- `apply_patch` 使用了两处都存在的重复上下文,匹配到第一处后插入,违反六文件只能追加到末尾的规则。

### 修复

- 改用唯一时间戳 `15:28:16` 区块作为真实 EOF 锚点。
- 在计划和笔记实际末尾追加 `18:20:10` 最终校正,不删除或改写已有历史。
- 后续追加必须使用末尾唯一标题或先读取 tail 确认锚点唯一性。

### 验证

- 最新 EOF 记录明确将 restart 决策计为一次,并保留已确认的 orphan cleanup 契约。
- 当前计划状态已推进到 permission prompt 决策。

## [2026-07-21 20:17:00] [Session ID: omx-1784512435044-92wxat] 错误修复: shell 检索命令引用失败

### 现象

- 错误码检索命令返回 `zsh: unmatched quote`。

### 原因

- 搜索 pattern 在双引号内包含反引号,zsh 将其解释为命令替换起始符。

### 修复

- 改用单引号包围 pattern 后重新执行。

### 验证

- 重试成功定位现有数字错误码约定: `64` 请求非法、`70` 服务端执行失败、`77` 权限不足、`78` backend 不支持。

## [2026-07-21 20:26:49] [Session ID: omx-1784512435044-92wxat] 错误修复: 恢复计划补丁上下文不精确

### 现象

- 首次追加恢复计划时,`apply_patch` 报告找不到预期末尾行。

### 原因

- 补丁上下文把原文 `Markdown 与` 写成了 `Markdown与`,少了一个空格。

### 修复

- 重新读取计划文件真实末尾,使用完整且唯一的 EOF 行作为追加锚点。

### 验证

- 首次失败没有修改任何文件。
- 修正后的补丁成功追加恢复计划,没有改写已有 append-only 记录。
